// Lua script to launch on boot
boot_script = "core/game/boot"

// Package to load on boot
boot_package = "boot"

window_title = "03-breakout"

// Linux-only configs
linux = {
	renderer = {
		resolution = [ 1024 1024 ]
		vsync = true
	}
}

// Windows-only configs
windows = {
	renderer = {
		resolution = [ 960 540 ]
		vsync = true
	}
}
